<!DOCTYPE html>
<html>
<head>
	<!-- Make site responsive on mobile/tablet -->
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<title>Lunch order</title>
	<link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
    <link rel="stylesheet" href="/stylesheets/login.css">
    <link rel="shortcut icon" href="http://res.cloudinary.com/dzi9oazgw/image/upload/v1517259641/favicon_xhllg3.jpg" type="image/x-icon" />
</head>
<body>

	<div class="container">
		<% if(error && error.length > 0){ %>
		<div class="alert alert-danger" role="alert">
			<%= error %>
		</div>
		<% } %>
		<% if(success && success.length > 0){ %>
		<div class="alert alert-success" role="alert">
			<%= success %>
		</div>
		<% } %>
	</div>

<div class="container">
	<div id="login-header" class="row">
		<h1 id="login-title">Lunch Time!</h1>
		<div id="login-form">
			<form action="/login" method="POST">
				<div class="form-group">
					<input id="username" class="form-control" type="text" name="username" placeholder="Your username">
				</div>
				<div>
					<input id="password" class="form-control" type="password" name="password" value="password" style="display: none;" >
				</div>
				<div class="form-group">
					<button class="btn btn-lg btn-primary btn-block">Enter</button>
				</div>
			</form>
		</div>
	</div>
</div>

</body>
</html>